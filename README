This its a Custom Edit for TukUI.

This edit instead to overwrite default installation its based on modified the current installation.

What its means its that your must install and donwload default tukui installation in the addons folders.

So any time that TukUI its update your could update it freely.

"e" infront of the numbers mean that its a external edit of tukui.
"A" that its a  alpha so not the final version.